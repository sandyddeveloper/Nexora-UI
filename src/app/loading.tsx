import { FullPageLoader } from '@/components/ui/loader';

export default function GlobalLoading() {
  return <FullPageLoader label="Loading Nexora OS..." sublabel="Preparing workspace layout & modules" />;
}
